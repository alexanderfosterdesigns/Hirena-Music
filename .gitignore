# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml
.idea/
.vscode/

# Rust (kept for the future Rust-core path)
rust/target/

# Local data (ARL vault, cache, DB) — never commit
*.arl
hirena_data/
.vault/

# OS
.DS_Store
Thumbs.db

# Misc
*.log

# CI workflow kept locally but not committed: the Arena GitHub App token
# lacks the `workflows` permission. Remove these lines and commit it from an
# account with that permission to enable CI.
.github/workflows/

# UI mockup renders (inspection artifacts — not source)
ui-mockups/covers/
ui-mockups/*.png

# Generated UI screenshots (from test/screenshots) — CI artifacts, not source
/screenshots/

# Bundled render archive
ui-renders.zip
